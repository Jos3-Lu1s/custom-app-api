<odoo>
    <!-- <record id="view_custom_app_api_token_list" model="ir.ui.view">
        <field name="name">custom_app_api.token.list</field>
        <field name="model">custom_app_api.token</field>
        <field name="arch" type="xml">
            <list string="API Tokens" editable="bottom">
                <field name="name"/>
                <field name="user_id"/>
                <field name="token" readonly="1"/>
                <field name="is_active"/>
            </list>
        </field>
    </record>

    <record id="action_custom_app_api_token" model="ir.actions.act_window">
        <field name="name">API Tokens</field>
        <field name="res_model">custom_app_api.token</field>
        <field name="view_mode">list</field>
    </record>

    <menuitem id="menu_custom_app_api_root" name="API Externa" sequence="100"/>
    <menuitem id="menu_custom_app_api_tokens" name="Tokens de Acceso" parent="menu_custom_app_api_root" action="action_custom_app_api_token"/> -->
</odoo>